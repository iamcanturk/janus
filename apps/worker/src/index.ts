/**
 * @janus/worker — queue consumer.
 *
 * Placeholder for Phase 0. Wires up to BullMQ and runs check tasks in Phase 2.
 */

import { CORE_PACKAGE } from '@janus/core';

function main(): void {
  console.log(`[janus] worker placeholder — depends on ${CORE_PACKAGE}`);
}

main();
