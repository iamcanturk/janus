/**
 * @janus/checks — all scanning modules, grouped by phase and mode.
 *
 * Placeholder for Phase 0. Passive checks (crt.sh, RDAP/ASN, DNS, Wayback,
 * InternetDB) land in Phase 3.
 */

export const CHECKS_PACKAGE = '@janus/checks';
